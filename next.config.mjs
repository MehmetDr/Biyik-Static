/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/Biyik-Static',
  assetPrefix: '/Biyik-Static/',
};

export default nextConfig;
